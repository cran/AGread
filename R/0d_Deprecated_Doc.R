# Functions ---------------------------------------------------------------

get_duration <- function(timer) {

  .Defunct(
    "get_duration",
    "PAutilities",
    "`get_duration` now lives in the PAutilities package"
  )

}
